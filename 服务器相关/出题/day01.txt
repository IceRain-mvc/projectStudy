选择题:
    1:npm run xxx 应该在哪个__文件下创建__脚本
      A:app.js  node
      B:package.json node
      C:app.js  script
      D:package.json script

      答案:D

    2:node不可以做什么事情:
      A:执行js文件
      B:当做服务器
      C:编写cli
      D:响应式编程

      答案:D

    3:哪个模块是加载网络的
      A:fs
      B:asset
      C:debug
      D:http

      答案:D

    4:--save-dev下载的依赖会放到package.json中的那个对象下:
      A:devDependencies
      B:dependencies
      C:devServer
      D:script

      答案:A

    5:关于 npm install xxx --save-dev 说法正确的是:
      A:会下载到生产环境的依赖
      B:会下载到开发环境的依赖
      C:和--save没有区别
      D:和npm install没有区别

      答案:B

    6:如果下载glup一般是下载到哪里
      A:开发环境的依赖
      B:生产环境的依赖
      C:直接npm install 就可以
      D:移上都不对

      答案:A

    7:node所用的引擎是什么
      A:渲染引擎
      B:JS引擎
      C:Chrome V8引擎
      D:移上都不对

      答案:C

    8:Nodejs模块化用的是什么规范
      A:Module
      B:export
      C:AMD
      D:CommonJs

      答案:D

    9:下列关于npm install 说法正确的是
      A:不可以配置远程代理
      B:不可以配置本地代理
      C:每次运行都是从远程registry下载
      D:新版本的npm是带有缓存的

      答案:D

    10:下列关于闭包作用错误的是:
      A:函数中能不包含闭包
      B:闭包指的是作用域范围
      C:如果不结束，就会一直占用内存，引起内存泄漏
      D:不用的时候就引用设为空

      答案:A

    11:

   多选题
    10:下列关于npm下载后路径说法正确的是:
       A:在 Windows 默认是%AppData%/npm-cache
       B:通过命令npm config get cache可以获取目录
       C:


    5:npm 配置命令是
    A:npm config

